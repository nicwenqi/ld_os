// Deliberately empty until a reviewed non-production Neon environment is ready.
// Adding an entry is a source-controlled operator authorization, never runtime input.
export const APPROVED_NEON_FIRST_INITIALIZATION_TARGETS = Object.freeze([]);
