"use client";import{useEffect,useMemo,useState}from"react";import{createRepositoryRegistry}from"../../repositories/registry.ts";import{deriveWizardState}from"../../services/initialization-wizard-service.ts";import"./initialization-status-card.css";
export function InitializationStatusCard(){const registry=useMemo(()=>createRepositoryRegistry(),[]);const[state,setState]=useState<ReturnType<typeof deriveWizardState>|null>(null);const[collapsed,setCollapsed]=useState(false);useEffect(()=>{let active=true;(async()=>{const hostname=registry.environment.dataMode==="mock"?"training-demo.example.test":registry.environment.devPropertyHostname??registry.environment.previewPropertyHostname??location.hostname;const context=await registry.property.resolveContext(hostname);if(!context)return;const[record,tree,positions,aliases,positionLabels,progress,batches]=await Promise.all([registry.property.getProperty(context.propertyId),registry.department.listTree(context.propertyId),registry.position.listPositions(context.propertyId),registry.department.listAliases(context.propertyId),registry.position.listSourceLabels(context.propertyId),registry.initialization.getProgress(context.propertyId),registry.import.listImportHistory(context.propertyId)]);if(active)setState(deriveWizardState({identity:record.identity,rules:record.settings,activeDepartments:tree.filter(node=>node.isActive).length,activePositions:positions.filter(position=>position.isActive).length,inspectedEmployeeMaster:batches.some(batch=>["mapping_required","validating","ready_for_review","importing","completed","completed_with_warnings"].includes(batch.status)),unresolvedDepartmentLabels:aliases.filter(alias=>alias.resolutionType==="deferred").length,unresolvedPositionLabels:positionLabels.filter(label=>label.resolutionStatus==="deferred").length,activePropertyAdministrator:true,progress}))})().catch(()=>{});return()=>{active=false}},[registry]);if(!state)return null;if(collapsed&&state.minimumReady)return<button className="setup-card-collapsed" onClick={()=>setCollapsed(false)}>初始化 {state.progressPercent}% · 查看运营准备状态</button>;return<section className={`initialization-status-card ${state.minimumReady?"minimum-ready":"minimum-blocked"}`}><div className="setup-score"><strong>{state.progressPercent}<small>%</small></strong><span>整体初始化</span></div><div className="setup-narrative"><span>{state.minimumReady?"酒店基础已可用":"酒店基础尚未就绪"}</span><h2>{state.operationalReady?"运营准备已完成":"正常工作台已开放，部分真实数据能力仍待准备"}</h2><p>{state.operationalReady?"所有初始化检查均已完成，可继续日常维护。":state.operationalBlockingReasons.slice(0,3).join(" · ")||"请完成酒店身份、业务规则、正式部门及管理员配置。"}</p><div><em className={state.minimumReady?"good":"risk"}>最低可用：{state.minimumReady?"已满足":"待完成"}</em><em className={state.operationalReady?"good":"watch"}>运营就绪：{state.operationalReady?"已完成":"进行中"}</em></div></div><nav><a className="primary" href="/initialize">继续初始化</a><a href="/settings/hotel">查看酒店设置</a><a href="/permissions#organization">查看组织架构</a><a href="/import">上传员工数据</a></nav>{state.minimumReady&&<button className="setup-collapse" onClick={()=>setCollapsed(true)} aria-label="收起初始化状态">收起</button>}</section>}
