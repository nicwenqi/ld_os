"use client";import{AppProviders}from"../providers";import{useAuthSession}from"../state/auth-session";import"../role-entry.css";function Denied(){const{session,status,logout}=useAuthSession();return<main className="role-entry"><section className="role-entry-card"><span>访问受限 · Access denied</span><h1>当前账号无权访问此酒店</h1><p>{status==="authenticated"?`${session.displayName??"当前账号"} 已登录，但没有当前酒店的有效成员资格或角色。`:"当前会话没有可用的酒店访问权限。"} 请联系酒店学习与发展管理员。</p><div className="role-entry-actions"><button className="primary" onClick={()=>void logout().then(()=>location.assign("/login"))}>退出登录</button></div></section></main>}export default function Page(){return<AppProviders><Denied/></AppProviders>}
