"use client";import Link from"next/link";import{AppProviders}from"../providers";import{SessionGate}from"../components/auth/SessionGate";import{useAuthSession}from"../state/auth-session";import"../role-entry.css";function Platform(){const{logout}=useAuthSession();return<SessionGate><main className="role-entry"><section className="role-entry-card"><span>平台安全入口 · Platform</span><h1>平台管理入口</h1><p>本检查点仅提供最小角色落地页。酒店创建、管理员分配与域名状态管理将在后续明确批准后实施。</p><div className="role-entry-actions"><Link href="/">查看酒店运营入口</Link><button onClick={()=>void logout().then(()=>location.assign("/login"))}>退出登录</button></div></section></main></SessionGate>}export default function Page(){return<AppProviders><Platform/></AppProviders>}
