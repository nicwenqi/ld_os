"use client";import {AppProviders}from"../providers";import{SessionGate}from"../components/auth/SessionGate";import{useAuthSession}from"../state/auth-session";import"../role-entry.css";function DepartmentHome(){const{session,logout}=useAuthSession();return<SessionGate><main className="role-entry"><section className="role-entry-card"><span>部门范围 · Department scope</span><h1>部门培训工作台</h1><p>{session.displayName}，这里将承载您被授权部门范围内的员工查看、培训提醒、补训、考勤与反馈管理。本检查点只恢复安全入口与范围说明。</p><div className="role-entry-grid"><article><strong>已授权范围</strong><small>前厅部及其下级部门（合成演示范围）</small></article><article><strong>不可操作</strong><small>酒店设置、全酒店导入、全局角色与其他部门数据</small></article></div><div className="role-entry-actions"><button onClick={()=>void logout().then(()=>location.assign("/login"))}>退出登录</button></div></section></main></SessionGate>}export default function Page(){return<AppProviders><DepartmentHome/></AppProviders>}
