import assert from "node:assert/strict";
import test from "node:test";
async function render(path="/"){const workerUrl=new URL("../dist/server/index.js",import.meta.url);workerUrl.searchParams.set("test",`${process.pid}-${Date.now()}-${path}`);const{default:worker}=await import(workerUrl.href);return worker.fetch(new Request(`http://localhost${path}`,{headers:{accept:"text/html"}}),{ASSETS:{fetch:async()=>new Response("Not found",{status:404})}},{waitUntil(){},passThroughOnException(){}})}
test("server renders the Chinese-first authentication boundary and branded login",async()=>{const root=await render();assert.equal(root.status,200);const rootHtml=await root.text();assert.match(rootHtml,/<html lang="zh-CN">/);assert.match(rootHtml,/<title>酒店学习与发展运营系统 \| Hotel L&amp;D OS<\/title>/);assert.match(rootHtml,/正在验证账户与酒店访问权限/);const login=await render("/login");assert.equal(login.status,200);const loginHtml=await login.text();assert.match(loginHtml,/用户 ID/);assert.match(loginHtml,/欢迎回来/);assert.doesNotMatch(`${rootHtml}${loginHtml}`,/codex-preview|react-loading-skeleton/)});
